{
  "name": "nextjs-v13",
  "private": true,
  "workspaces": [
    "./home",
    "./about",
    "./shared"
  ],
  "version": "0.0.0",
  "scripts": {
    "start": "concurrently \"npm --prefix fe-home run dev\" \"npm --prefix fe-about run dev\"",
    "build": "concurrently \"npm --prefix fe-home run build\" \"npm --prefix fe-about run build\"",
    "serve": "concurrently \"npm --prefix fe-home run start\" \"npm --prefix fe-about run start\""
  },
  "dependencies": {
    "concurrently": "7.0.0"
  }
}
